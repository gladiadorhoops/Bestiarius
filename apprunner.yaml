version: 1.0
runtime: nodejs14
build:
  commands:
    pre-build:
    - npm install -g @angular/cli 
    - npm install
    build:
    - npm run-script build
run:
  command: npm start
  network:
    port: 2400
